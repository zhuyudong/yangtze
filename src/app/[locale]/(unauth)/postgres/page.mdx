# 项目常用 SQL

```sql
SELECT * FROM public.content where id = 'clvanlu1p00u08h1gwk3n0md6'
ORDER BY id ASC

SELECT * FROM public.content where likes > 0 or favorites > 0 or no_interesteds > 0
ORDER BY id ASC
```

## tsx

```tsx
@import('./Alerts.tsx')
```
