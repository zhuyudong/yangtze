## MongoDB 常用脚本

#### 批量添加数据

新增列并添加默认数据

```js
db.collection_name.updateMany(
  {},
  {
    $set: {
      platform: 'atlas'
    }
  }
)
```

#### 重命名列名

```js
db.collection_name.updateMany({}, { $rename: { scene_config_id: 'config_id' } })
```
